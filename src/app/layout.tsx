import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Razorpay ResQ — Autonomous AI Revenue Recovery Engine",
  description:
    "Autonomous multi-agent payment failure interception, smart bank failover routing, and personalized customer nudge orchestration for Razorpay.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-[#0E1117] text-[#FAFAFA]">
        {children}
      </body>
    </html>
  );
}
